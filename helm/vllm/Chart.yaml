apiVersion: v2
name: vllm
description: Wrapper chart for vllm-stack plus TEI CPU embeddings and Whisper
type: application
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: vllm-stack
    version: 0.1.10
    repository: https://raw.githubusercontent.com/vllm-project/production-stack/gh-pages
